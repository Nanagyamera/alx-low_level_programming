include "main.h"
/**
 * _islower - function to check for lowercase charater
 * @c: the argument int that will be used for the argument of the function
 * Return: Success
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
		retun (0);
}
