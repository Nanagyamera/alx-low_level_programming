include "main.h"

/**
 * _islower - function to check for lowercase charater
 * @c: a character argument
 * Return: returns 1 and 0 depending on the condition
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
